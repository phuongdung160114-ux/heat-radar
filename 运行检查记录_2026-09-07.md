**HEAT RADAR 2.0.0 运行检查记录**

检查日期：2026-09-07，Windows x64 / Python 3.13。

后续更新：本记录中两个东方财富接口的断连问题已完成专项修复，最新结果见文末。

解压位置：`F:\炒股\HEAT_RADAR_2.0.0`。原压缩包保留，56 个源文件全部解压；修改了其中 8 个文件。项目独立运行环境已建立在 `.venv`，可双击同目录的 `启动热度雷达_Windows.cmd`。首次实际使用会建立 `data` 目录。

**发现并修复的问题**

| 问题 | 原有影响 | 处理结果 |
|---|---|---|
| 5 个新增采集函数未加入允许列表 | 证券主表、互动易、关注指数及主营构成被程序自身拒绝 | 补齐明确的允许列表；27 个函数均存在于安装版本，调用参数核对通过 |
| 高级配置页面与后台字段不一致 | 原样点击保存也返回 400 | 接受并校验 max_candidates、analysis_history_days；网页实际保存成功 |
| 参数与业务关系缺少类型校验 | 错误字符串、零平滑参数、空敏感性对象等保存后，使分析出现类型或除零错误 | 在保存前校验；74 个错误参数用例通过，合法映射保存、分析、关闭关系通过 |
| 多步骤采集丢失已成功结果 | 主营构成已经取得，但随后个股概况失败时，前面的记录也被丢弃；关注详情、回填存在同类问题 | 各接口分别处理失败，保留成功数据与失败步骤；10 个故障注入检查通过，回填新增数量及重复计数正确 |
| Windows 安装探测中断 | 在 Windows PowerShell 5.1 中，探测不存在的 Python 版本可能直接终止安装 | 允许继续寻找已安装版本；启动、安装与依赖修复统一使用本目录已有独立环境；启动入口与脚本语法检查通过 |
| 全市场资讯分类严重缓慢 | 五千多只股票反复触发正则表达式编译，20 条资讯的完整原版采集入库耗时约 161 秒 | 分类器保存编译结果；单条真实资讯分类由 4.686 秒降至 0.0495 秒，完整结果一致 |

修改文件：`radar/ak_worker.py`、`radar/settings.py`、`radar/engine.py`、`radar/providers.py`、`radar/data_sources.py`、`radar/classify.py`、`installers/install.ps1`、`installers/start.ps1`。

**实际验证结果**

- 37 个 Python 文件语法、2 个 JavaScript 文件语法、Windows 脚本语法及全部模块导入通过；`pip check` 未发现依赖冲突。
- 第一轮 47 项本地接口与操作检查通过，覆盖初始化、查询、导入去重、筛选、详情、导出、反馈、映射、标签、研究任务和数据库备份。合并修复后的 19 个页面与查询入口及主要写入操作复查通过，数据库完整性检查通过。
- 浏览器实际打开主页、详情、研究实验、关系图与配置页面；最终从启动入口打开页面，保存高级配置、生成 38 个初始观察对象成功；浏览器未记录错误或警告。
- 使用本次公开采集的数据副本，修复版处理并入库 200 条东方财富快讯耗时 11.95 秒；对 5,583 个对象完成整体分析耗时 13.692 秒。
- 分类优化通过 11,760 次词条比较及 270 次完整分类比较；修复版分类 20 条真实资讯、173 个事件耗时 1.421 秒，首次准备词条另需 0.822 秒。
- 逻辑回归、岭回归、LambdaMART 分别完成 120 条内存样本训练和 40 条样本预测，包含缺失值；结果均为有限且非恒定数值。LightGBM 模型文本重新加载预测通过。
- 原版 200 条快讯的慢速对照任务在问题定位后结束；修复版已利用同一批公开数据完成验证。

**公开数据接口抽查**

| 接口 | 本次结果 |
|---|---|
| 沪深京证券主表 | 成功，5,558 行 |
| 互动易 | 成功，17 行 |
| 用户关注指数 | 成功，30 行 |
| 主营构成 | 成功，200 行 |
| 雪球关注榜 | 成功，标准化为 11,108 条观察记录 |
| 雪球讨论榜 | 成功，标准化为 11,120 条观察记录 |
| 财联社电报 | 成功，20 条 |
| 东方财富财经快讯 | 成功，200 条 |
| 东方财富人气榜 | 远端断开连接，RemoteDisconnected |
| 东方财富个股概况 | 远端断开连接，RemoteDisconnected；修复后此步骤失败可保留先前取得的主营构成 |

**仍存在的运行边界**

- 两个东方财富接口本次未取得数据。补齐代码只能解决本地调用缺陷，无法保证网站持续响应；本次没有修改 VPN 或网络配置。
- 内置交易日历覆盖 2025-01-01 至 2026-12-31。2027 年开始需要补充交易所确认的日历，否则采集和分析会因日期超出范围而失败；现有生成脚本也只包含 2025、2026 两年。
- 初始包没有真实历史数据库，新安装后需要持续采集，才能形成可比历史与成熟研究标签。
- 本次抽查了上述 10 个公开入口，未逐一运行所有数据源的所有分页、股票和历史区间；需要凭据的 Tushare、X、Reddit 及供应商服务未做授权调用。
- 模型检查验证了实际训练和预测函数的兼容性；完整跨期研究实验及长期运行稳定性尚未经过持续观测。
- Python 已在本机安装，因此未执行下载并安装全新 Python 的分支；已验证版本缺失时继续探测的逻辑。

依赖与字段核对资料：[AKShare 官方股票接口文档](https://akshare.akfamily.xyz/data/stock/stock.html)、[AKShare PyPI](https://pypi.org/project/akshare/)、[Uvicorn 0.48.0 PyPI](https://pypi.org/project/uvicorn/0.48.0/)。

所有测试数据库、原始数据副本、测试页面、临时文件和字节码缓存已清理；保留运行所需的独立环境。正式数据目录尚未建立，测试数据不会混入后续使用。

原压缩包 SHA256：`2BE3F526BDFE23C101FE561CCE3C8F4FB0D849EA389995F0325B1A7FAD884D81`。

**东方财富接口专项修复：2026-09-07 21:06**

逐步记录原版请求后确认：人气榜的 `emappdata.eastmoney.com/stockrank/getAllCurrentList` 正常返回 100 名股票，失败发生在随后访问 `push2.eastmoney.com/api/qt/ulist.np/get` 补充股票名称、价格的步骤。个股概况访问同一行情主机的 `/api/qt/stock/get`，出现同样的远端断连。原版人气榜把排名与附加行情绑定，附加请求失败就使整榜失败。

不同请求头、字段组合及连接库的对照出现过短暂成功，随后再次断连，因此这些变化不足以构成稳定修复。实际确认的是旧行情入口不稳定；仅凭断连不能判断服务端内部原因，也不能据此认定 IP 被封。公开项目已有同类记录：[AKShare 问题 7051](https://github.com/akfamily/akshare/issues/7051)。

增加项目内的 `radar/eastmoney.py`，只接管这两个函数，保留已安装 AKShare 及其他函数。行情请求先尝试原入口，连接异常、服务器错误或数据不合格时，切换到东方财富自己的 `push2delay.eastmoney.com`。明确的授权拒绝和限流响应不触发切换。请求有超时上限，证券代码和返回字段需要匹配。

人气排名始终来自原人气榜；补充名称按照证券代码对应，避免上游返回顺序变化导致错配。即使两个名称入口都失败，也保留真实排名及本库已有名称，并标记名称缺失。补充地址、采集时刻、主入口失败记录和可用的行情时间随数据保存；不把备用入口的行情声称为新的实时排名。

个股概况只请求实际需要的字段，保存行业、上市日期、流通市值及实际来源。对方未提供有效行情时间时保持空值。

实际验证：

- 独立子进程连续两次取得完整人气榜：各 100 名、100 个不同代码，名称齐全，耗时约 1.43 秒和 0.98 秒；两次均成功切换备用入口。
- 平安银行、国电南瑞的概况各返回 9 项资料，行业、上市日期和市值有效。
- 程序完整采集同时启用人气榜与主营构成：3.09 秒内保存 300 条记录，两个来源均为 `ok`，失败数为 0。国电南瑞概况本轮通过备用入口取得，资料与实际来源已保存到临时库；随后完成 133 个对象的分析和数据库完整性检查。
- 16 项断连、返回错序、证券代码不匹配、无效参数、空榜、全部入口失败及授权拒绝检查通过。

本轮修改前的三个既有文件保存在 `东方财富修复前_20260907.zip`；新增文件为 `radar/eastmoney.py`。未修改 VPN、系统代理或其他数据源的请求方式。测试数据库、原始响应副本和临时目录已删除，未向正式数据库写入测试记录。
